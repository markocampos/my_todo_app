name: TODO ni Mark
description: A minimalist productivity app for nursing and dev students.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0 # For that minimalist, premium typography
  intl: ^0.19.0        # For date formatting

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
